<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h2 class="marker">商品一覧</h2>
      <div class="public-products">
          <% @products.each do |product| %>
        <div class="public-products1">
              <%= link_to product_path(product.id) do %>
                  <%= attachment_image_tag product, :image, size:'235x180' ,  fallback: "cake.jpg" %>
              <% end %>

                <p><%= link_to product.name, product_path(product) %></p>
                <h3 class="product-price">¥<%= product.excluding_tax_price %></h3>
        </div>
          <% end %>
      </div>
          <%= link_to '上部に戻る', products_path %>
          <%= paginate @products %>
    </div>
  </div>
</div>
