<h2>ジャンル編集</h2>
  <%= form_with model:@product, local:true do |f| %>
    <div class="form-group">
      <h4>ジャンル名</h4>
      <%= f.text_field :name, autofocus: true, id:"inputTitle", placeholder:"name"%>
    </div>
    <div class="form-group">
      <%= f.submit "変更を保存" %>
    </div>
  <% end %>