<h2>ジャンル編集</h2>
  <%= form_with model:@admin_product, local:true do |f| %>
    <h4>ジャンル名</h4>
    <%= f.text_field :name, autofocus: true, id:"inputTitle", placeholder:"name"%>
    <%= f.submit "変更を保存" %>
  <% end %>
  
 